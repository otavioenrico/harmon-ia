// Copie este arquivo para `config.js` e preencha com os dados do SEU projeto
// Supabase (Project Settings → API). A anon key é pública por design — quem
// protege os dados é a Row Level Security do banco, não o segredo da chave.
//
//   cp assets/js/config.js{.example,}   ← no terminal, a partir da raiz... quase:
//   cp assets/js/config.example.js assets/js/config.js
//
// config.js está no .gitignore e não vai pro repositório.

export const SUPABASE_URL = "https://SEU-PROJETO.supabase.co";
export const SUPABASE_ANON_KEY = "SUA_ANON_KEY_AQUI";
